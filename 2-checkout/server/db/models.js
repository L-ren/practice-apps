// models contains methods to CRUD info with database
const db = require('./db.js')

module.exports.add = () => {
  console.log(`add method called in models`);
  // return a promise object retrieving from db
};

module.exports.get = () => {
  console.log(`get method called in models`);
};